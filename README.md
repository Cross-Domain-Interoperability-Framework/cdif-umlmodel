# cdif-umlmodel
This repository holds the UML models that define CDIF, and serve as the source for the syntactic implementations of each profile. There are associated repositories for the syntax outputs (cdif-modeloutput) and for the configuration information (cdif-productionconfiguration) used in the transformation from model to syntax artefact. 
